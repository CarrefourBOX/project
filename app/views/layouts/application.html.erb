<%= render 'layouts/base' do %>
  <%= render 'shared/navbar' unless current_page? root_path %>
  <div id="content-wrap" class='<%= 'home' if current_page? root_path %>'>
    <%= yield %>
  </div>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= render 'shared/menu' %>
<% end %>