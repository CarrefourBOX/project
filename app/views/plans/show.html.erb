<div class="row d-flex justify-content-center">
      <div class="card text-dark bg-light mb-3">
        <div class="text-center card-header"> Meu Carrinho
        </div>
        <div class="card-body">
          <%= "#{@plan.category} - #{humanized_money_with_symbol @plan.price} (início: #{@plan.created_at.strftime("%d/%m/%Y")})" %>
          <%= button_to 'Confirmar Pagamento', checkout_create_path, method: :post, params: {id: @plan.id}, remote: true %>
        </div>
      </div>
    </div>
